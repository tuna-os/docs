---
sidebar_position: 1
sidebar_label: "Pantheon shortcuts"
title: "Pantheon keyboard shortcuts"
---

# Pantheon keyboard shortcuts

Gurnard uses the Pantheon desktop from elementary OS. These shortcuts are
different from GNOME's defaults, and can be changed in **System Settings →
Keyboard → Shortcuts**. On a Mac keyboard, `⌘` means the **Super** key; on most
PC keyboards it is the Windows key.

## Window management

| Shortcut | Action |
| --- | --- |
| `Alt` + `Tab` | Cycle through open windows |
| `Alt` + `F7` | Move the active window with the pointer |
| `Alt` + `F8` | Resize the active window with the pointer |
| `⌘` + `↓` | Open Multitasking View (window and workspace overview) |
| `⌘` + `←` / `→` | Move to the previous or next workspace |
| `⌘` + `1` … `9` | Jump to a numbered workspace |
| `⌘` + `0` | Create or jump to a new workspace |
| `⌘` + `Alt` + `←` / `→` | Move the active window to the previous or next workspace |

You can also maximize a window by dragging its header bar to the top of the
screen or double-clicking the header bar. The Multitasking View and workspace
shortcuts can be customized from **System Settings → Keyboard → Shortcuts →
Workspaces**.

## Applications

| Shortcut or action | Result |
| --- | --- |
| Press `⌘`, then type | Open the Applications Menu and search for an app or setting |
| Press `⌘`, then search for **Shortcuts** | Open the desktop shortcut overlay |
| Click an app in the Dock | Focus the app, or launch it if it is not running |

There is no guaranteed dedicated default shortcut for launching Gedit or
Terminal. Search for either app from the Applications Menu, or add a custom
shortcut in **System Settings → Keyboard → Shortcuts**. Gurnard's installed
application set may also use different names—for example, an image may provide
elementary's **Code** editor instead of Gedit.

`⌘` + `Space` is not a universal Pantheon application-launch shortcut. On
current elementary defaults it may be assigned to keyboard-layout switching;
check **System Settings → Keyboard → Layout** before relying on it.

## Notifications and indicators

Notifications do not have a dedicated default keyboard shortcut in the
elementary shortcut list. Select the notification/system-indicator area on the
right side of the Panel to view notifications and related controls, including
Do Not Disturb when available.

## Finding and changing shortcuts

Press `⌘` to open the Applications Menu, search for **Shortcuts**, and open the
first matching result. From the shortcut overlay, choose **Keyboard Settings**
to view or change desktop shortcuts. The same settings are available under
**System Settings → Keyboard → Shortcuts**.

Shortcut behavior can vary with the keyboard layout, connected hardware, and
Pantheon version. The local shortcut overlay is authoritative for the Gurnard
image.

Sources: [elementary OS — Learning the
Basics](https://elementary.io/docs/learning-the-basics) and the
[Gurnard variant page](/gurnard).
